<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appperizinan">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_ku_square"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_ku"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyListDataStatus"></activity>
        <activity android:name=".terimaIzin" />
        <activity android:name=".MyListDataAdmin" />
        <activity android:name=".MyListData" />
        <activity android:name=".daftarIzinAdmin" />
        <activity android:name=".adminPage" />
        <activity android:name=".izinBuat" />
        <activity android:name=".mahaPage" />
        <activity android:name=".loginAdmin" />
        <activity android:name=".loginMaha" />
        <activity
            android:name=".ui.login.LoginMAHASISWA"
            android:label="@string/title_activity_login_mahasisw" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>